#!/bin/bash

DATASETS="blood rectangles elipses german_credit pima heart normal_3_class"
N_PARTICLES=40
N_ITERS=5

for dataset in $DATASETS; do

    echo "Running for ${dataset}..."
    python ciel.py -d "$dataset" -n "$N_ITERS" -p "$N_PARTICLES"

done
