#!/bin/bash

DATASETS="blood rectangles german_credit elipses heart contraceptive pima wdbc australian_credit wine iris"
N_PARTICLES=30
N_ITERS=10

for dataset in $DATASETS; do

    echo "Running for ${dataset}..."
    python ciel.py -d "$dataset" -n "$N_ITERS" -p "$N_PARTICLES"

done
